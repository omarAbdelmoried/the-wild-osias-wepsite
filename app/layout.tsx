import "./_styles/globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

import { ReservationProvider } from "../features/reservation/context/ReservationContex";

import { Josefin_Sans } from "next/font/google";

const josefin = Josefin_Sans({ subsets: ["latin"], display: "swap" });

export const metadata = {
  title: {
    template: "%s/The Wild Oasis",
    default: "The Wild Oasis",
  },
  description: "Generated by Next.js",
  viewport: {
    width: "device-width",
    initialScale: 1,
  },
};
export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${josefin.className} mx-auto antialiased bg-primary-950 text-primary-100 min-h-screen flex flex-col`}
      >
        <Header />
        <hr className="border-primary-100 mx-auto z-30 w-2/4" />
        <div
          className="flex-1 px-4 sm:px-6 md:px-8 py-6 sm:py-8 md:py-12
        "
        >
          <main className="max-w-7xl mx-auto ">
            <ReservationProvider>{children}</ReservationProvider>
          </main>
        </div>
        <Footer />
      </body>
    </html>
  );
}
